general:
  build_dir: src
  
checkout:
  post:
    - echo $CIRCLE_TAG $CIRCLE_BRANCH $CIRCLE_BUILD_NUM $CIRCLE_USERNAME
    - ../scripts/build.sh $CIRCLE_TAG $CIRCLE_BRANCH $CIRCLE_BUILD_NUM $CIRCLE_USERNAME
    - head -32 ../felix-*/src/felix/felixrefine.f90
    
compile:
  override:
    - cd ../felix-*
    - make clean; make

test:
  post:
    - cd ../samples/GaAs_short
    - ./felix